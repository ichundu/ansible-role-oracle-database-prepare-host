# {{ ansible_managed }}
# Setting the appropriate ulimits for '{{ oradb_os_user[0].name }}' user

if [ $USER = "{{ oradb_os_user[0].name }}" ]; then
    if [ $SHELL = "/bin/ksh" ]; then
        ulimit -u 16384
        ulimit -n 65536
    else
        ulimit -u 16384 -n 65536
    fi
fi
